<%= form_with(model: positive, local: true) do |form| %>
  <% if positive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(positive.errors.count, "error") %> prohibited this positive from being saved:</h2>

      <ul>
      <% positive.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :lat %>
    <%= form.text_field :lat %>
  </div>

  <div class="field">
    <%= form.label :long %>
    <%= form.text_field :long %>
  </div>

  <div class="field">
    <%= form.label :lonlat %>
    <%= form.text_field :lonlat %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
